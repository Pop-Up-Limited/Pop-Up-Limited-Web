---
import { COMPANY_NAME, FOOTER_COPYRIGHT, SOCIAL_LINKS, SUPPORT_EMAIL } from '../config/site';
import { translations } from '../i18n/translations';
---

<footer class="border-t border-white/10 bg-black/80 backdrop-blur-md">
  <div class="mx-auto flex w-full max-w-6xl flex-col gap-10 px-4 py-12 md:px-6 md:py-16">
    <div class="flex flex-col gap-6 md:flex-row md:items-start md:justify-between">
      <div class="max-w-sm space-y-3">
        <span class="text-xs uppercase tracking-widest text-brand-400">Pop Up Limited</span>
        <h3 class="text-2xl font-semibold text-white">
          {COMPANY_NAME}
        </h3>
        <p class="text-sm text-gray-400" data-i18n="footer.description"></p>
      </div>

      <div class="grid w-full grid-cols-1 gap-10 text-sm md:max-w-md">
        <div class="space-y-2">
          <h4 class="font-semibold text-white" data-i18n="footer.support"></h4>
          <ul class="space-y-2 text-gray-400">
            <li>
              <a class="hover:text-brand-400" href="/support" data-i18n="footer.helpCenter"></a>
            </li>
            <li>
              <a class="hover:text-brand-400" href={`mailto:${SUPPORT_EMAIL}`}>
                {SUPPORT_EMAIL}
              </a>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <div class="flex flex-col gap-6 border-t border-white/10 pt-6 text-sm text-gray-400 md:flex-row md:items-center md:justify-between">
      <p data-i18n="footer.copyright">{FOOTER_COPYRIGHT}</p>

      <ul class="flex items-center gap-4">
        {SOCIAL_LINKS.map((item) => (
          <li>
            <a
              href={item.href}
              class="flex h-10 w-10 items-center justify-center rounded-full border border-white/20 text-gray-400 transition hover:border-brand-400 hover:text-brand-400 focus-visible:ring-2 focus-visible:ring-brand-400"
              aria-label={item.label}
            >
              <span class="text-xs font-semibold uppercase">{item.icon}</span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </div>
</footer>

<script define:vars={{ translations }}>
  // Get language from storage or URL
  function getLanguage() {
    if (typeof window === 'undefined') return 'zh';
    const stored = localStorage.getItem('language');
    const urlLang = new URLSearchParams(window.location.search).get('lang');
    return (urlLang === 'en' || urlLang === 'zh') ? urlLang : (stored === 'en' || stored === 'zh') ? stored : 'zh';
  }
  
  // Update footer texts
  function updateFooterTexts(lang) {
    const t = translations[lang];
    if (!t) return;
    
    document.querySelectorAll('footer [data-i18n]').forEach((el) => {
      const key = el.getAttribute('data-i18n');
      if (key) {
        const keys = key.split('.');
        let value = t;
        for (const k of keys) {
          value = value?.[k];
        }
        if (value && typeof value === 'string') {
          el.textContent = value;
        }
      }
    });
  }
  
  // Make function globally accessible
  if (typeof window !== 'undefined') {
    window.updateFooterTexts = updateFooterTexts;
  }
  
  // Initialize on page load
  if (typeof window !== 'undefined') {
    document.addEventListener('DOMContentLoaded', () => {
      const lang = getLanguage();
      updateFooterTexts(lang);
    });
    
    // Listen for language change events
    window.addEventListener('languagechange', function(e) {
      const event = e;
      const newLang = event.detail && event.detail.lang;
      if (newLang) {
        updateFooterTexts(newLang);
      }
    });
  }
</script>

